# 性能报告（PRD §23.6）

- 生成时间：2026-09-16 02:56:57 UTC
- 生成方式：`make perf`（后端 `scripts/benchmark.py` + 前端 `next build` 产物）
- 口径：分位数用最近秩（rank = ceil(q·n)），**样本量一并列出来了** ——
  n=10 的 p95 就是最大的那个样本，n=20 的 p95 是第二大的那个；样本这么少时
  不要把它当统计学结论读。耗时优先取响应里的 `meta.elapsed_ms`（后端计时）。
- 「结论」一列里 **⏭ 未测 / 不可测 不是达标**：没测到的门槛一律显式写出来，
  而不是留空或打勾（本项曾经把没测过的 TTI 与没设阈值的并发都渲染成 ✅）。

## 门槛一览

| 指标 | 目标 | 方法 | 实测 | 结论 |
| --- | --- | --- | --- | --- |
| 规划（冷启动，无搜索） | ≤ 8s | 10 个不同参数 | 202 ms | ✅ |
| 规划（缓存命中） | ≤ 1.5s | 同一参数重复 20 次 | 244 ms | ✅ |
| 修改（自然语言改路线） | ≤ 3s | 20 次修改 | 263 ms | ✅ |
| 规划（含搜索） | — | 3 个需搜索场景 | — | ⏭ 不可测 |
| 并发 20 个冷规划 | 无 5xx / 无死锁 | asyncio.gather(20)，参数各不相同 | 5259 ms | ✅ |
| 束搜索（80 候选，三种 archetype） | < 500ms | 领域层纯计算，不含数据库/序列化 | 82 ms | ✅ |
| 最慢的单条 SQL（PRD：0 条 > 200ms） | ≤ 200ms | SQLAlchemy 事件计时（本机没装 pg_stat_statements 扩展） | 35 ms | ✅ |
| 分享页 LCP | ≤ 2.5s | Playwright + CDP 节流（4G、CPU 4x、移动端 viewport），取 3 次中位数 | 384 ms | ✅ |
| 首页 TTI | ≤ 3s | 未测（需长任务分析） | — | ⏭ 不可测 |
| 首屏 JS（首页 First Load JS） | ≤ 200KB gzip | `next build` 输出的 gzip 后体积 | 127.0 KB | ✅ |

## 明细

### 规划（冷启动，无搜索）

- 方法：10 个不同参数
- 样本 n=10 · 最小 190 ms · 中位 206 ms · **p95 307 ms** · 最大 307 ms

### 规划（缓存命中）

- 方法：同一参数重复 20 次
- 样本 n=19 · 最小 244 ms · 中位 244 ms · **p95 244 ms** · 最大 244 ms

### 修改（自然语言改路线）

- 方法：20 次修改
- 样本 n=20 · 最小 2 ms · 中位 243 ms · **p95 354 ms** · 最大 361 ms

### 规划（含搜索）

- 方法：3 个需搜索场景
- 样本 n=0（该项无法有效测量，理由见下）
- 说明：**不可测**：这 3 条场景在规划链里一次搜索都没发生（cost_logs.category='search' 计数未变）。原因：L8 未接进规划链 —— `plan_service` 只驱动 L1/L3/L4/L7，全仓没有任何地方请求 KIND_SEARCH（TASKS.md 已记为刻意决定：接它等于让本地库不足时自动联网）。场景本身实测 246ms, 246ms, 246ms，但那是普通规划，不能当『含搜索』用。搜索 Provider 自身的行为由 `tests/integration/test_search_live.py` 覆盖。

### 并发 20 个冷规划

- 方法：asyncio.gather(20)，参数各不相同
- 样本 n=1 · 最小 5259 ms · 中位 5259 ms · **p95 5259 ms** · 最大 5259 ms
- 说明：全部 20 条耗时 5.3s；5xx = 0；注：in-process ASGI，不含网络与 uvicorn 开销

### 束搜索（80 候选，三种 archetype）

- 方法：领域层纯计算，不含数据库/序列化
- 样本 n=3 · 最小 81 ms · 中位 82 ms · **p95 108 ms** · 最大 108 ms
- 说明：候选 80（candidate_max=80）· 关系 104 条 · relaxed 82.4ms、classic 107.91ms、themed 81.2ms

### 最慢的单条 SQL（PRD：0 条 > 200ms）

- 方法：SQLAlchemy 事件计时（本机没装 pg_stat_statements 扩展）
- 样本 n=1 · 最小 35 ms · 中位 35 ms · **p95 35 ms** · 最大 35 ms
- 说明：顺序阶段：993 条语句 · 合计 744 ms · 超过 200ms 的 0 条 · 最慢一条 35 ms。并发阶段的争用单独列在下面，不计入这条门槛

### 分享页 LCP

- 方法：Playwright + CDP 节流（4G、CPU 4x、移动端 viewport），取 3 次中位数
- 样本 n=1 · 最小 384 ms · 中位 384 ms · **p95 384 ms** · 最大 384 ms
- 说明：样本 [384, 380, 384] ms · 页面 http://127.0.0.1:3100/t/baAtTy0Iy7XH。用 CDP 而不是 Lighthouse CLI：仓库已有 Playwright 与 Chrome，不为一行指标再装第二个浏览器；代价是没有 Lighthouse 的可访问性/SEO 审计。

### 首页 TTI

- 方法：未测（需长任务分析）
- 样本 n=0（该项无法有效测量，理由见下）
- 说明：**未测**：TTI 的正确定义是「首个 5s 静默窗之前最后一个长任务结束」，需要长任务分析，本轮没做 —— 宁可不写，也不拿 domInteractive 之类的东西顶替。

### 首屏 JS（首页 First Load JS）

- 方法：`next build` 输出的 gzip 后体积
- 样本 n=1 · 最小 127.0 KB · 中位 127.0 KB · **p95 127.0 KB** · 最大 127.0 KB
- 说明：首页 First Load JS 127.0 kB（其中 shared 102.0 kB，已含在内，不重复相加）；各页面 First Load JS 见下表。单位均为 gzip 后

- 冷规划的缓存层分布：None×10
- 缓存命中的响应元信息：`{'cached': True, 'cache_layer': None}`

## 其他核对项

- **开着 LLM 的冷规划（仅观察，不计入门槛）**：n=3 · 中位 250 ms · 最大 253 ms（门槛用的是关掉 LLM 的规则引擎路径）
- **SQL 统计 · 顺序阶段（冷规划/缓存命中/修改）**：990 条 · 合计 727.9 ms · 最慢 35.1 ms · 超 200ms 0 条
- **SQL 统计 · 并发 20 阶段（SQL 争用）**：580 条 · 合计 62618.3 ms · 最慢 1993.3 ms · 超 200ms 62 条
- **SQL 统计 · 束搜索阶段（纯计算，仅取数）**：3 条 · 合计 15.7 ms · 最慢 13.5 ms · 超 200ms 0 条
- **并发 20 的响应码**：[200, 200, 200, 200, 200, 200, 200, 200, 200, 200, 200, 200, 200, 200, 200, 200, 200, 200, 200, 200] · 墙钟 5259.2 ms
- **束搜索候选数**：80
- **测量限制**：in-process ASGI（不含网络/uvicorn）；本机为开发机，数字用于回归对比而不是容量规划
- **首页 LCP（观察值，不是 TTI）**：样本 [2756, 2568, 2536] ms · 中位 2568 ms。因 TTI ≥ LCP 恒成立，这个读数就是 **TTI 的下界**：LCP 越接近 3s 门槛，TTI 越需要单独确认（要长任务分析）。首屏瓶颈是 Hero 视频。
- **各页面 First Load JS（kB）**：/ 127.0, /_not-found 103.0, /about/data 109.0, /amap-proxy/[...path] 102.0, /dev 112.0, /explore/[city] 106.0, /me 109.0, /t/[slug] 115.0, /t/[slug]/opengraph-image 102.0, /trip/[id] 118.0
