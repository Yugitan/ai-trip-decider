# 本机环境实测：registry.npmjs.org 不可达（连接超时），registry.npmmirror.com 正常。
# 这不是项目偏好，而是**环境约束** —— 未配置镜像时 pnpm install / create-next-app 会挂住。
# 如果所在网络能直连 npmjs.org，可以删掉本文件。
registry=https://registry.npmmirror.com
strict-ssl=true
# Next.js 的构建产物较大，提高并发拉取超时容忍度
fetch-timeout=120000
fetch-retries=3
