# 前端环境变量（Next.js 只读 `frontend/` 下的 .env* 文件）
#
# 用法：复制本文件为 `frontend/.env.local` 并填写。`.gitignore` 里 `.env.*` 已忽略它，
# 只有本文件（`.env.example`）会被提交。完整说明见 RUNNING.md §8。

# ── 后端地址 ───────────────────────────────────────────────────────────────
# 注意有**两个**名字，作用不同：
#   API_BASE_URL            （无前缀 = 服务端）：rewrites 的转发目标，以及 SSR/RSC
#                           取公开数据时的绝对地址。写在这里才生效 —— Next 读不到根 .env。
#   NEXT_PUBLIC_API_BASE_URL（有前缀 = 浏览器）：一般**不要设**，理由见下。
#
# API_BASE_URL=http://127.0.0.1:8000
#
# 默认留空 = 浏览器只请求同源 `/api/*`，由 next.config.ts 的 rewrites 转发给后端。
# 这是推荐做法：跨站直连会让后端签发的游客会话 cookie（td_session）被浏览器当第三方
# cookie 丢掉，于是 GET /trips/{id} 永远 403（详见 RUNNING.md §8.6）。
# 只在确实要连别的后端时才设置它。
# NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# ── 首页背景视频（可选）────────────────────────────────────────────────────
# 留空则用代码里内置的默认资源。
# NEXT_PUBLIC_HERO_VIDEO_URL=

# ── 高德地图（结果页的站点示意图）──────────────────────────────────────────
# 需要在高德开放平台申请 **Web端(JS API)** 类型的 Key 与配套安全密钥。
# 注意：这一对与后端的 `AMAP_WEB_KEY`（.env，Web服务类型）**不是同一个 Key**，
# 用错类型会返回 USERKEY_PLAT_NOMATCH (10009)。
#
# JS API Key 必须带 NEXT_PUBLIC_ 前缀：它要出现在浏览器加载的脚本 URL 里，
# 这是 JS API 的固有形态（保护手段是高德控制台的域名白名单），
# 也是本项目「Key 只存在于后端」这条铁律的**唯一已知例外**。
NEXT_PUBLIC_AMAP_JS_KEY=

# 安全密钥**刻意不加 NEXT_PUBLIC_ 前缀**：它只在服务端使用，
# 由 app/amap-proxy 在转发 /_AMapService/* 时补上 `jscode`，不进前端产物。
# 高德官方把「代理服务器转发」列为强烈建议（安全）的方式，明文写在页面里则不建议用于生产。
AMAP_SECURITY_CODE=
