# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
.mutmut_cache/

# ── Node / Next.js ──────────────────────────────────────────────────────────
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
next-env.d.ts

# ── Playwright ──────────────────────────────────────────────────────────────
test-results/
playwright-report/
blob-report/
.playwright/

# ── 环境与密钥（绝不入库）───────────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key

# ── 抓取到的原始数据 ────────────────────────────────────────────────────────
# 两份 OSM 原始 JSON（合计约 3.8MB）**刻意入库**：集成测试缺数据是 fail 而不是 skip，
# CI 必须在没有 Overpass、不跑 9 分钟抓取的前提下复现真实知识库（见 .github/workflows/ci.yml）。
# 代价：`make fetch-osm` 重跑会改动这两份已跟踪的文件 —— 这是预期的，「数据变了」应当被看见。
backend/data/raw/*.json
!backend/data/raw/osm_guangzhou_raw.json
!backend/data/raw/osm_guangzhou_curated_extras.json
!backend/data/raw/osm_guangzhou_transport.json
!backend/data/raw/.gitkeep

# ── 本地工具的工作目录（不是项目内容）───────────────────────────────────────
.workbuddy-ai/
# 一次性数据探测/抓取的临时现场（证据会誊写进 TASKS.md，现场本身不入库）
.scratch/
# `make perf` 的原始证据（next build 日志 / LCP 读数 / perf.json / 服务日志）：
# 结论誊写进 docs/PERF_REPORT.md，现场本身每次重跑都会变，因而不入库。
.perf/
# 手动验证 gitleaks 时的下载现场（二进制 + sums + report.json）：
# 结论誊写进 TASKS.md，现场本身不入库 —— 报告里躺着假 Key 的原文，不该被提交。
.tmp-gitleaks/

# ── 系统 ────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.log
logs/
